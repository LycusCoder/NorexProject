═══════════════════════════════════════════════════════════════════════════
              📘 NourProject CLI Tools - Complete Guide
═══════════════════════════════════════════════════════════════════════════

🎯 ONE COMMAND FOR EVERYTHING: bash nour.sh [command]

═══════════════════════════════════════════════════════════════════════════
📋 GETTING STARTED (Urutan yang Benar)
═══════════════════════════════════════════════════════════════════════════

1️⃣  FIRST TIME ONLY - Setup
    ────────────────────────────────
    $ bash nour.sh setup
    
    ✅ Apa yang dilakukan:
       • Check Docker & Python
       • Install GUI dependencies (PySide6, psutil)
       • Build Docker images
       • Setup directories
       • Set permissions
    
    ⏱️  Waktu: ~5-10 menit (pertama kali saja)

2️⃣  START SERVICES
    ────────────────────────────────
    $ bash nour.sh start
    
    ✅ Apa yang dilakukan:
       • Start Apache (port 8080)
       • Start MySQL (port 3306)
       • Start phpMyAdmin (port 8081)
    
    ⏱️  Waktu: ~30 detik

3️⃣  VERIFY (Optional tapi direkomendasikan)
    ────────────────────────────────
    $ bash nour.sh verify
    
    ✅ Apa yang dilakukan:
       • Check semua requirement
       • Verify file structure
       • Test Docker services
       • Check network connectivity
       • Health status report
    
    ⏱️  Waktu: ~5 detik

4️⃣  LAUNCH GUI atau OPEN BROWSER
    ────────────────────────────────
    Option A - GUI Desktop (Recommended):
    $ bash nour.sh gui
    
    Option B - Browser:
    $ bash nour.sh open
    
    ✅ Siap coding! 🚀

═══════════════════════════════════════════════════════════════════════════
📚 ALL COMMANDS REFERENCE
═══════════════════════════════════════════════════════════════════════════

🔹 QUICK START
┌─────────────────────────────────────────────────────────────────────────┐
│ bash nour.sh setup         First-time installation & dependencies       │
│ bash nour.sh gui           Launch GUI desktop application               │
│ bash nour.sh start         Start all Docker services                    │
│ bash nour.sh stop          Stop all services                            │
│ bash nour.sh restart       Restart all services                         │
└─────────────────────────────────────────────────────────────────────────┘

🔹 SERVICE MANAGEMENT
┌─────────────────────────────────────────────────────────────────────────┐
│ bash nour.sh status        Show service status (running/stopped)        │
│ bash nour.sh logs          View all service logs                        │
│ bash nour.sh shell         Open bash shell in Apache container          │
└─────────────────────────────────────────────────────────────────────────┘

🔹 PHP MANAGEMENT
┌─────────────────────────────────────────────────────────────────────────┐
│ bash nour.sh php 8.1       Switch to PHP 8.1                            │
│ bash nour.sh php 8.2       Switch to PHP 8.2                            │
│ bash nour.sh php 8.3       Switch to PHP 8.3                            │
│ bash nour.sh php-info      Show current PHP version                     │
└─────────────────────────────────────────────────────────────────────────┘

🔹 DATABASE
┌─────────────────────────────────────────────────────────────────────────┐
│ bash nour.sh db-backup     Backup MySQL database                        │
│ bash nour.sh db-shell      Open MySQL command line (password: 041201)   │
└─────────────────────────────────────────────────────────────────────────┘

🔹 PYTHON CLI (Docker-based, No host install needed!)
┌─────────────────────────────────────────────────────────────────────────┐
│ bash nour.sh python                    Interactive Python 3.11+ shell   │
│ bash nour.sh python 'print("Hi")'      Execute Python code             │
│ bash nour.sh python script.py          Run Python script file          │
└─────────────────────────────────────────────────────────────────────────┘

🔹 UTILITIES
┌─────────────────────────────────────────────────────────────────────────┐
│ bash nour.sh verify        Health check & verification                  │
│ bash nour.sh open          Open main site in browser (port 8080)        │
│ bash nour.sh pma           Open phpMyAdmin in browser (port 8081)       │
│ bash nour.sh reset         ⚠️  Reset environment (destroys data!)        │
│ bash nour.sh update        Update NourProject via git pull              │
└─────────────────────────────────────────────────────────────────────────┘

🔹 INFORMATION
┌─────────────────────────────────────────────────────────────────────────┐
│ bash nour.sh info          System information & status                  │
│ bash nour.sh version       Version information                          │
│ bash nour.sh help          Show help menu                               │
└─────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════
💼 WORKFLOW EXAMPLES
═══════════════════════════════════════════════════════════════════════════

🌅 MORNING - Starting Work
────────────────────────────────────────────────────────────────────────────
# Check if everything OK
$ bash nour.sh verify

# Start services
$ bash nour.sh start

# Launch GUI (keep it running all day)
$ bash nour.sh gui

# Or just open in browser
$ bash nour.sh open

────────────────────────────────────────────────────────────────────────────

🔧 DEVELOPMENT - Common Tasks
────────────────────────────────────────────────────────────────────────────
# Check PHP version
$ bash nour.sh php-info

# Switch PHP if needed
$ bash nour.sh php 8.3

# Run Python script for automation
$ bash nour.sh python deploy.py

# Check service status anytime
$ bash nour.sh status

# View logs if something wrong
$ bash nour.sh logs

# Access database
$ bash nour.sh db-shell

────────────────────────────────────────────────────────────────────────────

💾 BACKUP - Before Major Changes
────────────────────────────────────────────────────────────────────────────
# Backup database
$ bash nour.sh db-backup

# Backup saved with timestamp in project root
# Example: nour_db_backup_2024-11-03_12-30-45.sql

────────────────────────────────────────────────────────────────────────────

🌆 EVENING - End of Day
────────────────────────────────────────────────────────────────────────────
# Optional: Stop services (or leave running)
$ bash nour.sh stop

# Services will auto-restart on next boot anyway

────────────────────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════════════════
🎓 ADVANCED USAGE EXAMPLES
═══════════════════════════════════════════════════════════════════════════

📝 PYTHON AUTOMATION EXAMPLES
────────────────────────────────────────────────────────────────────────────
# Quick calculation
$ bash nour.sh python 'print(sum(range(1, 101)))'
Output: 5050

# Check if web server is up
$ bash nour.sh python 'import requests; r=requests.get("http://localhost:8080"); print(r.status_code)'
Output: 200

# Parse JSON
$ bash nour.sh python 'import json; print(json.dumps({"status":"ok"}, indent=2))'

# Interactive data analysis
$ bash nour.sh python
>>> import pandas as pd
>>> df = pd.read_csv('/workspace/data.csv')
>>> print(df.head())

────────────────────────────────────────────────────────────────────────────

🗄️  DATABASE OPERATIONS
────────────────────────────────────────────────────────────────────────────
# Backup before migration
$ bash nour.sh db-backup

# Access MySQL shell
$ bash nour.sh db-shell
mysql> SHOW DATABASES;
mysql> USE nour_db;
mysql> SHOW TABLES;
mysql> SELECT * FROM users;
mysql> exit

# Or via Python
$ bash nour.sh python
>>> import mysql.connector
>>> conn = mysql.connector.connect(
...     host="localhost",
...     user="root",
...     password="041201",
...     database="nour_db"
... )
>>> cursor = conn.cursor()
>>> cursor.execute("SHOW TABLES")

────────────────────────────────────────────────────────────────────────────

🐚 CONTAINER SHELL ACCESS
────────────────────────────────────────────────────────────────────────────
# Access Apache container
$ bash nour.sh shell

# Inside container - install packages
root@container:/# composer require guzzlehttp/guzzle
root@container:/# apt-get update && apt-get install vim
root@container:/# exit

────────────────────────────────────────────────────────────────────────────

🔄 PHP VERSION TESTING
────────────────────────────────────────────────────────────────────────────
# Test app on PHP 8.1
$ bash nour.sh php 8.1
$ bash nour.sh open
# Test your app...

# Switch to PHP 8.2
$ bash nour.sh php 8.2
$ bash nour.sh open
# Test again...

# Back to PHP 8.3
$ bash nour.sh php 8.3

────────────────────────────────────────────────────────────────────────────

═══════════════════════════════════════════════════════════════════════════
🐛 TROUBLESHOOTING COMMANDS
═══════════════════════════════════════════════════════════════════════════

Problem: Services won't start
Solution:
$ bash nour.sh verify              # Check what's wrong
$ bash nour.sh logs                # View error logs
$ bash nour.sh restart             # Try restart

Problem: GUI won't launch
Solution:
$ bash nour.sh setup               # Reinstall dependencies
$ pip3 install PySide6 psutil      # Manual install

Problem: Port conflict
Solution:
$ bash nour.sh gui                 # Launch GUI
# Click ⚙️ → Ports → Change port → Save
$ bash nour.sh restart

Problem: Database connection failed
Solution:
$ bash nour.sh status              # Check if MySQL running
$ bash nour.sh db-shell            # Test connection
$ bash nour.sh logs | grep mysql   # Check MySQL logs

Problem: Can't access website
Solution:
$ bash nour.sh status              # Check services
$ bash nour.sh verify              # Full health check
$ curl http://localhost:8080       # Test connection

Problem: Docker not running
Solution:
# Linux:
$ sudo systemctl start docker
# Mac/Windows:
# Start Docker Desktop

═══════════════════════════════════════════════════════════════════════════
💡 PRO TIPS
═══════════════════════════════════════════════════════════════════════════

1. CREATE ALIAS (Optional)
   ────────────────────────────────────────────────────────────────────────
   Add to ~/.bashrc or ~/.zshrc:
   
   alias nour="bash /app/nour.sh"
   
   Then use:
   $ nour start
   $ nour gui
   $ nour python
   
   Much shorter! ⚡

2. DAILY HEALTH CHECK
   ────────────────────────────────────────────────────────────────────────
   $ bash nour.sh verify
   
   Run this setiap pagi untuk memastikan everything OK!

3. KEEP GUI RUNNING
   ────────────────────────────────────────────────────────────────────────
   Launch GUI di pagi hari, minimize to system tray.
   Right-click kapan saja untuk quick actions!

4. AUTOMATE WITH PYTHON
   ────────────────────────────────────────────────────────────────────────
   Create scripts di /app/ dan run dengan:
   $ bash nour.sh python myscript.py
   
   Project directory auto-mounted at /workspace in Docker!

5. REGULAR BACKUPS
   ────────────────────────────────────────────────────────────────────────
   $ bash nour.sh db-backup
   
   Backup before:
   • Major database changes
   • Testing migrations
   • Updating production

═══════════════════════════════════════════════════════════════════════════
📞 NEED MORE HELP?
═══════════════════════════════════════════════════════════════════════════

📖 Documentation:
   /app/docs/                              All documentation
   /app/GETTING_STARTED.md                 Quick start guide
   /app/docs/guides/PHASE_3_QUICK_START.md Phase 3 features
   /app/docs/guides/PHASE_3_COMPLETE.md    Complete guide

🔍 Quick Commands:
   bash nour.sh help                       Command reference
   bash nour.sh verify                     Health check
   bash nour.sh info                       System info

═══════════════════════════════════════════════════════════════════════════

🎉 You now have complete control over NourProject!

One command does it all: bash nour.sh [command]

Happy Coding! 🚀

═══════════════════════════════════════════════════════════════════════════
